# Project Rule

- Run the full `BuildAll` target for verification.
- In project documentation, write paths as relative paths from the project root.
- On Windows, when a command involves Chinese text, prefer using a Node.js script instead of handling the text inline in shell commands.
